import { responsiveStyle } from 'styled-system'
import withBox from './withBox';

export const flex = responsiveStyle('flex')
export const order = responsiveStyle('order')

export default withBox('div');
